{% extends 'base.html.twig' %}

{% block title %}Entreprise{% endblock %}

{% block body %}
    <h1>Entreprise</h1>

<div class="card border-success mb-3" style="max-width: 25rem;">
  <div class="card-header">ENTREPRISE : ID: {{ entreprise.id }}<br>
    <h4 class="text-warning"> {{ entreprise.raisonSociale }}  </h4>
  </div>
  <div class="card-body text-success">
    <h5 class="card-title">SIRET : {{ entreprise.siret }}</h5>
    <p class="card-text">ADRESSE : <br>{{ entreprise.adresse }}<br> {{ entreprise.cp }}<br> {{ entreprise.ville }}</p>
  </div>
</div>
<div style="display: flex;">
    <a style=" margin-right:1%; width: 10%" href="{{ path('entreprise_index') }}" class="btn btn-primary">back to list</a>
    <a style=" margin-right:1%; width: 10%" href="{{ path('entreprise_edit', {'id': entreprise.id}) }}" class="btn btn-primary">edit</a>

    {{ include('entreprise/_delete_form.html.twig') }}
</div><br><br>
{% endblock %}

    

    
